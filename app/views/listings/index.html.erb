<h1>Listings</h1>

<!--
<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Type</th>
      <th>Name</th>
      <th>Price</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @listings.each do |listing| %>
      <tr>
        <td><%= @user.find(listing.user_id).username.capitalize %></td>
        <td><%= @type.find(listing.type_id).name %></td>
        <td><%= link_to listing.name, listing %></td>
        <td><%= "$"+number_with_precision(listing.price, precision: 2) %></td>
        <td><%= listing.description %></td>
        <% if user_signed_in? && listing.user_id == current_user.id %>
          <td><%= link_to 'Edit', edit_listing_path(listing) %></td>
          <td><%= link_to 'Remove Listing', listing, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

-->
<%= link_to 'New Listing', new_listing_path %>


<% @listings.each do |listing| %>
  <h4><%= listing.name %></h4>
  <%= image_tag listing.footage, width: '400px' %>
  <br>
  <%= listing.description %>
  <p>Price: <%= "$"+number_with_precision(listing.price, precision: 2) %></p>
  <%= link_to 'Buy Now', listing %>
<% end %>
